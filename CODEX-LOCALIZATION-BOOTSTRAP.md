# Codex 한글화 프로젝트 부트스트랩

이 문서는 새 게임 한글화 프로젝트를 시작하거나 기존 프로젝트를 재정비할 때 Codex가 가장 먼저 읽는 **상위 진입점**입니다.

**사용자는 한글화 작업을 시작할 때 이 문서 링크 하나만 전달해도 됩니다.**
Codex는 이 문서를 읽은 뒤 프로젝트별 규칙과 필요한 세부 문서를 스스로 확인하고, 아래의 모델 라우팅 정책까지 적용하여 작업을 진행합니다.

기존 `GPT-GITHUB-OPERATIONS.md` 또는 `switch/SWITCH-KOREAN-LOCALIZATION-GUIDE.md`를 대체하지 않습니다. 이 문서는 프로젝트 운영 규칙과 검수 구조를 연결하고, 실제 플랫폼과 작업 대상에 맞는 세부 문서를 선택해서 읽도록 안내합니다.

---

## 링크 하나만 받은 Codex의 기본 동작

사용자가 이 문서 링크와 함께 한글화 작업을 지시했다면 다음 순서로 바로 진행합니다.

```text
1. 이 문서를 읽는다
2. 대상 게임이 신규 프로젝트인지 기존 프로젝트인지 판정한다
3. 신규 프로젝트라면 다른 조사·추출·문서 작성보다 먼저 D:\Codex\한글화 프로젝트\<게임명> 전용 폴더를 생성하고 그 폴더를 프로젝트 루트로 고정한다
4. 기존 프로젝트라면 현재 프로젝트 루트를 그대로 사용하고 폴더 구조를 소급 재편하지 않는다
5. 프로젝트 AGENTS.md와 현재 상태를 읽는다
6. 실제 파일과 기존 성공 자산을 조사한다
7. Sol High가 소장처럼 작업을 분해하고 위험도·난이도를 판단한다
8. 비자명한 전문 실무는 DevSpace + Web GPT-5.6 Sol XHIGH를 기본 책임자로 배차하고, 안전한 단순 작업만 Luna에 배차한다
9. 고난도 구현·디버깅은 Astra, 최고난도 난제는 DevSpace + Web GPT-6 Pro로 올린다
10. Sol High가 작업자 결과를 재검증하고 충돌을 해결해 최종 통합한다
11. 실제 결과를 확인하기 전에는 완료라고 보고하지 않는다
```

사용자가 매 작업마다 모델을 직접 지정할 필요는 없습니다.
**Sol High가 기본 총괄자로서 자동으로 배차합니다.**

작업에 필요한 정보가 이미 프로젝트와 이 문서에 있다면 사용자가 같은 내용을 다시 설명하도록 요구하지 않습니다.

---

## 핵심 원칙

```text
조사 → 단일 샘플 → 실기 검증 → 전체 적용 → 자동 검증 → 변경분만 배포
```

아카이브의 문서는 범용 노하우입니다. 실제 대상 프로젝트의 파일, 현재 구현, 성공 샘플, 해시, 런타임 결과보다 우선하지 않습니다.

---

## 신규 프로젝트는 게임 전용 폴더부터 만든다

대상 게임이 정해졌고 아직 해당 게임의 한글화 프로젝트 루트가 없다면, **실제 조사·추출·문서 작성·도구 제작을 시작하기 전에 먼저 게임 전용 프로젝트 폴더를 생성합니다.**

기본 경로:

```text
D:\Codex\한글화 프로젝트\<게임명>
```

예:

```text
D:\Codex\한글화 프로젝트\이상한 환상향 로터스 Lotus Labyrinth
```

게임명이 확정된 시점부터 이 폴더를 해당 작품의 **유일한 로컬 작업 루트**로 취급합니다. 이후 그 게임에 속하는 다음 자료는 원칙적으로 이 프로젝트 루트 안에서 생성·관리합니다.

- 조사 메모와 분석 보고서
- 프로젝트 문서와 `AGENTS.md`
- 추출·변환·재삽입 작업 자료
- `work` / analysis / staging 성격의 중간 산출물
- 빌더·패처·검증 도구와 테스트 자료
- 게임 전용 검수 프로그램 및 그 설정·기준자료
- 빌드·패치·배포 준비 산출물
- 런타임·실기 검증 기록과 실패/성공 증거

**프로젝트 폴더를 만들기 전에 바탕화면, 사용자 홈, 공용 임시 작업 폴더, 다른 게임 프로젝트 안에 게임별 작업물을 먼저 만들어 흩뿌리지 않습니다.** 도구나 운영체제가 내부적으로 사용하는 일시적 캐시/임시파일은 예외지만, 재사용할 프로젝트 산출물이나 증거는 반드시 게임 전용 루트로 회수해 관리합니다.

다른 게임의 프로젝트 폴더를 임시 작업장으로 빌려 쓰지 않습니다. 공통 코드나 범용 문서를 참조할 수는 있지만, 게임별 `work`, staging, 원장, 검수 프로그램, 빌드 결과, 테스트 기록을 서로 섞지 않습니다.

기존 프로젝트가 이미 존재하면 이 규칙을 이유로 폴더를 새로 만들거나 이름을 바꾸거나 구조를 강제 재배치하지 않습니다. **신규 프로젝트 생성 시점부터 적용하고, 기존 프로젝트는 현재 루트를 그대로 존중합니다.**

---

## 최초 한글 출력의 필수 합격 관문: 다음 장면까지 진행되는가

최초 한글 출력 샘플을 만들 때 반드시 [workflow/EARLY-TEXT-RUNTIME-GATE.md](workflow/EARLY-TEXT-RUNTIME-GATE.md)를 적용합니다. **한글이 보이는 것만으로 한글화 가능 판정을 내리지 않습니다. 대량 번역 전에 안전한 출력·진행 경로를 먼저 확인합니다.**

초반에 확인할 것:

- 가로 최대 픽셀 폭과 조건부 표시 글자 수: 한글·영문·숫자·공백·혼합 문구, 실제 승인 폰트·크기·자간 기준. 가변폭을 하나의 고정 글자 수로 일반화하지 않습니다.
- 대사창 종류별 세로 최대 줄 수, 자동 줄바꿈, 경계 초과 시 잘림·겹침·멈춤.
- `[n]` 유지·삭제·이동·추가·개수 변경 각각의 출력과 다음 대사·다음 장면 진행 안전성.
- 반각/전각 영문·숫자·문장부호·공백 등의 변경 전후 코드 포인트·게임 인코딩 바이트·레코드/제어 구조와 진행 결과.
- RSC의 실제 제한 대상·단위, 긴 한글·변수 치환, 음성·대기·선택지·장면 전환 동작.

원본 실행과 무변경 재삽입본을 기준선으로 삼고, 복구 가능한 시험 사본에서 한 조건씩 바꿉니다. **출력 성공, 구조/RSC 통과, 진행 성공은 별도 판정**입니다.

`[n]` 변경이나 반각/전각 변환 후 진행이 깨지면 해당 동작을 금지하고 정상 기준선의 검증된 조건을 보존합니다. 미검증도 자동 허용하지 않습니다. 전각↔반각 일괄 변환, Unicode 정규화, 공백 trim·축약, 문장부호 자동 교정은 허용 범위가 입증되기 전에는 수행하지 않습니다.

결과는 게임별 `layout_profile` 또는 기존 동등 자료에 **표시 한계 + 동작별 허용/금지/미검증 + 진행 증거 + 규격 버전**으로 저장하고 검수 프로그램과 Google Drive 기준자료에 연결합니다. 이후 1차·2차·3차의 지시는 이 허용 범위 안에서만 적용합니다. 3차라는 이유로 보호된 `[n]`이나 문자 폭 조건을 바꾸지 않습니다.

착수 판정은 **진행 가능 / 조건부 진행 / 기술 보강 후 재판정**으로 나눕니다. 보존 조건에서 정상 진행되면 제한을 명시한 조건부 한글화가 가능합니다. 삭제 실패 하나로 작품 전체를 불가능이라 단정하지 않으며, 안전한 경로를 못 찾았는데 번역량부터 밀어붙이지도 않습니다.

이 기술 관문과 용어집·캐릭터·관계·말투의 언어 0단계는 둘 다 본 번역의 선행 조건입니다. 조사·기준자료 구축은 병행할 수 있습니다. **1차 1,000행 / 2차 1,000행 / 3차 500행은 그대로 유지**하고, 초반 기술 시험을 매 검수 묶음에 반복 전가하지 않습니다. 기존 프로젝트의 성공 자료·사용자 확정값은 보존하고 부족한 시험만 보강합니다.

문서 반영은 게임 시험 완료나 검사기 구현 완료를 뜻하지 않습니다. 실제 시험·수정은 프로젝트별 승인 범위에서 수행합니다.

---

## Codex 작업 시작 순서

1. 현재 작업 대상 게임이 신규인지 기존인지 확인합니다.
2. 신규 게임이고 프로젝트 루트가 아직 없으면 **다른 실작업보다 먼저** `D:\Codex\한글화 프로젝트\<게임명>` 폴더를 생성하고 그 경로를 프로젝트 루트로 고정합니다. 기존 프로젝트라면 현재 루트를 그대로 사용합니다.
3. 대상 프로젝트에 `AGENTS.md`가 있으면 가장 먼저 읽고 그 프로젝트의 규칙을 우선합니다.
3. 실제 프로젝트 파일과 현재 상태를 조사합니다. 다른 게임에서 성공한 방식만 보고 구조를 추정하지 않습니다.
5. `workflow/PROJECT-OPERATING-MODEL.md`를 읽습니다.
6. `workflow/SOL-LUNA-AGENT-POLICY.md`를 읽습니다. 파일명은 과거 호환을 유지하지만 현재는 **Sol / Astra / Luna / Web GPT 전체 라우팅 정책**입니다.
7. 실제 플레이 기반 수정과 검수 프로그램 정책이 필요하면 `workflow/PLAYTEST-FEEDBACK-POLICY.md`를 읽습니다.
8. 대량 대사 번역이나 검수 프로그램용 번역 기준자료를 준비할 때는 먼저 [번역 전 기준자료·용어집 선세팅 정책](review/PRE-TRANSLATION-SETUP.md)을 읽고 0단계 합격 게이트를 확인합니다.
9. 실제 대사 번역·언어 검수에는 [대사 번역 공통 원칙](review/DIALOGUE-TRANSLATION-POLICY.md)을 읽습니다. 검수 작업자는 게임별 공통 기준과 현재 차수의 지시문만 사용하며, 다른 차수의 지시문을 혼합하지 않습니다. 번역 파이프라인을 구축할 때는 `review/REVIEW-DATA-SCHEMA.md`도 확인합니다.
10. 플랫폼별 기술 노트는 실제 대상과 일치할 때만 선택해서 읽습니다.
   - Nintendo Switch: `switch/SWITCH-KOREAN-LOCALIZATION-GUIDE.md`
11. 실제 공개 작업 시작을 선언하는 프로젝트라면 `workflow/PUBLIC-WORKLOG-STANDARD.md`를 읽고 루트 `WORKLOG.md`와 README의 `🚧 한국어화 작업 진행 중` 안내를 준비합니다. 후보 조사 단계에는 생성하지 않으며, 이미 최종 배포가 끝난 `Dollars-Archive/eve-rebirth-terror-kr-patch`와 `Dollars-Archive/eve-ghost-enemies-kr-patch`에는 소급 적용하지 않습니다.
12. 이미 프로젝트에서 성공한 빌더, 샘플, 패치 방식, 승인된 번역 자산은 근거 없이 다시 설계하지 않습니다.
13. 위험한 변경은 전체 적용 전에 조사 또는 단일 대표 샘플부터 수행합니다.
14. 단일 샘플이 정적 검증뿐 아니라 실제 런타임에서 성공한 뒤에 전체로 확장합니다.
15. 게임별 데이터, 관계도, 말투, 용어집, 오프셋, 패치 규칙은 다른 게임에서 자동으로 가져오지 않습니다.
16. 산출물은 재현 가능한 도구, 테스트, manifest, 해시, 검증 기록을 남기는 방향으로 만듭니다.
17. 실제 결과를 다시 읽거나 테스트하기 전에는 완료했다고 판단하지 않습니다.

---

## 프로젝트 분리 원칙

로컬 작업도 게임별 전용 루트를 먼저 만들고 그 안에서 분리합니다. 신규 프로젝트의 기본 루트는 `D:\Codex\한글화 프로젝트\<게임명>`이며, 한 게임의 조사·문서·work·검수 프로그램·테스트·산출물을 다른 게임 프로젝트와 섞지 않습니다.

기본 단위는 다음과 같습니다.

```text
게임 1개
  → Codex 한글화 프로젝트 1개
  → 게임 전용 검수 프로그램 1개
  → 게임 전용 Google Drive 검수 작업공간 1개
```

공통 코드는 재사용할 수 있지만 다음 항목은 게임별로 독립 관리합니다.

- 대사 데이터
- 용어집
- 캐릭터 이름과 성격
- 캐릭터 말투
- 화자 → 청자 관계도
- 반말/존댓말과 호칭 규칙
- 검수 이력
- 실제 플레이 확정값
- 바이너리 포맷 가정과 오프셋
- 승인된 번역과 UI 자산
- 공개 작업일지 (`WORKLOG.md`)

공개 작업일지는 사용자에게 보여주는 진행 현황판이며, 게임별 내부 기술 기록이나 재현 가능한 제작 노하우 문서와 동일시하지 않습니다. 공개 범위는 `workflow/PUBLIC-WORKLOG-STANDARD.md`를 따릅니다.

---

## 역할 분담

Codex는 주로 **한글화 엔지니어링**을 담당합니다.

- 게임 파일/자산 분석
- 텍스트 추출·재삽입
- 폰트/UI/영상/바이너리 작업
- 빌더·패처·검증 도구 제작
- 게임별 검수 프로그램 제작
- 검수 프로그램에 등록할 기준자료의 구조·스키마·완성 상태 확인
- 테스트와 정적 검증
- staging·build·release 산출물 생성
- 최종 검수 CSV 반입

대량 번역과 단계별 언어 검수는 주로 **웹 ChatGPT + Google Drive CSV**에서 수행합니다.

---

# 번역 전 0단계: 기준자료와 용어집을 먼저 만든다

실제 대사 대량 번역을 시작하거나 게임별 검수 프로그램에 본 번역 기준자료를 등록하기 전에 [review/PRE-TRANSLATION-SETUP.md](review/PRE-TRANSLATION-SETUP.md)를 적용합니다.

**용어집·캐릭터·관계·말투를 먼저 세팅하고 번역을 시작합니다.** 번역을 다 끝낸 뒤 반말/존댓말·호칭·캐릭터 말투·고유명사를 전수 수정하는 방식을 기본 흐름으로 삼지 않습니다.

## 0단계 필수 기준자료

최소한 다음을 게임별로 준비합니다.

- 캐릭터명·별칭·칭호
- 아이템·던전·시스템·스펠·기술·세계관의 반복 핵심 용어
- 캐릭터 성격·기본 말투·1인칭·말버릇
- 화자 → 청자 방향 관계도
- 반말/존댓말 기준
- 호칭 기준
- 각 기준의 출처와 `confirmed / provisional / HOLD` 상태

A → B와 B → A는 다른 규칙일 수 있으므로 관계도를 방향별로 관리합니다.

## 캐릭터 이름과 성격·말투 조사

캐릭터 이름의 우선순위는 다음과 같습니다.

```text
사용자 확정값
→ 공식 홈페이지·공식 게임·공식 설정 자료
→ 공식 자료가 일본어뿐이고 한국어 표기가 애매하면 나무위키의 통용 표기 참고
→ 그래도 부족하면 일본 위키·팬 위키·사전·작품 자료 교차 확인
→ 끝까지 애매하면 HOLD
```

캐릭터 성격과 말투는 공식 소개, 실제 게임 일본어 대사, 매뉴얼·설정집·공략본, 당시 연재 기사·인터뷰·특집 자료를 적극 조사합니다. 오래된 작품은 나무위키와 일본 위키의 인물·관계 정리도 보조자료로 사용할 수 있습니다. 다만 실제 게임 일본어 원문이나 공식 자료와 충돌하면 후자가 우선합니다.

## 일본어 원문과 영어판

일본어판과 번역된 영어판이 모두 있으면 **대사·용어·존대·호칭·캐릭터 말투의 기준은 일본어 원문**입니다.

영어판은 의미 확인, 문맥 보조, 고유명 철자 확인용입니다. 영어판에서 추가·축약·순화·과장된 설명, 농담, 욕설, 비유를 일본어 원문보다 우선하지 않습니다.

## 0단계 합격 게이트

다음 핵심 기준이 준비되지 않았으면 검수 프로그램의 본 번역 기준자료 등록 또는 대량 대사 번역을 완료 상태로 진행하지 않습니다.

- 주요 캐릭터명 표기
- 주요 반복 용어
- 주요 캐릭터 성격·말투
- 주요 화자 → 청자 관계도
- 반말/존댓말과 호칭 기준
- 일본어 원문과 영어판의 authority 구분
- 미확정 HOLD 목록
- 주요 확정값의 근거 출처

핵심 등장인물과 빈출 용어가 비어 있는데 번역량부터 밀어붙이지 않습니다.

## 검수 패스와 고정 작업 단위

```text
0단계: 기준자료·용어집 구축
→ 1차: 전체 대사집을 1,000행 단위로 기초 검수
→ 2차: 1차 결과 전체를 다시 1,000행 단위로 자연화·캐릭터 말투 검수
→ 3차: 2차 결과 전체를 다시 500행 단위로 가장 꼼꼼한 최종 정밀검수
```

**1차·2차·3차는 서로 다른 행 구간을 나눠 맡는 것이 아닙니다. 각 차수에서 전체 대사집을 다시 검수합니다.** 마지막 잔여 블록을 제외하면 기본 묶음 크기 1,000 / 1,000 / 500을 유지합니다.

---

# 대사 번역과 한국어 줄바꿈

실제 번역을 시작할 때는 [review/DIALOGUE-TRANSLATION-POLICY.md](review/DIALOGUE-TRANSLATION-POLICY.md)를 적용합니다. 모델·파일·패치 운영 규칙만 지키고 언어 원칙을 생략하지 않습니다.

## 대사 번역의 공통 주의사항

- 앞뒤 문맥, 화자·청자, 관계·존대·호칭과 캐릭터 말투를 확인합니다.
- 일본어 어순을 기계적으로 복제하지 않되, 원문에 없는 주어·설명·정보를 임의로 추가하지 않습니다.
- 의미, 감정 강도, 복선, 의도된 모호함, 농담과 장면 기능을 보존합니다. 자연화와 재창작을 혼동하지 않습니다.
- 용어집과 사용자 확정값을 우선하며, 사용자 실플레이 확정 번역·배치는 자동으로 뒤집지 않습니다.
- 원문·행 ID·보호 필드·음성·대기·페이지·분기·변수 명령을 지킵니다. 불확실한 해석과 미확인 기술 조건은 근거와 함께 표시합니다.

## 검수 단계별 배치 책임

| 단계 | 우선 작업 | 줄바꿈 처리 |
|---|---|---|
| 1차 1,000행 | 기초 규칙 생성·확인, 구조·관계·존대·용어 판정 | `[n]` 재배치와 전 행 픽셀 정밀 검사를 맡기지 않음 |
| 2차 1,000행 | 자연스러운 한국어와 캐릭터 말투 완성 | 원문 `[n]` 위치에 얽매이지 않고 번역하되 최종 화면 배치는 미확정 |
| 3차 500행 | 의미·뉘앙스 최종 정밀검수 후 화면 배치 확정 | 최종 문장 기준 `[n]` 제거·재배치·생성 및 픽셀 폭·최대 줄 수 검사 |

**해당 게임에서 순수한 화면 줄바꿈이며 제거·이동이 안전하다고 검증된 `[n]`만 번역 필드에서 재배치합니다.** 원문 필드는 그대로 보존합니다. 미확인 태그와 페이지·대기·음성·연출 명령은 이 예외에 포함하지 않습니다. 사용자 확정 줄바꿈도 자동 변경하지 않습니다.

위 단계표의 제거·재배치·생성은 최초 출력 시험에서 해당 동작과 다음 장면 진행이 각각 허용된 경우에만 적용합니다. 반각/전각·공백 보존 규칙도 함께 따르며, 실패·미검증 변환은 최종 검수라는 이유로 수행하지 않습니다.

## 출력 규격 검증은 번역 차수와 별도

Codex는 실제 가로 픽셀 한계, 최대 표시 줄 수, 자동 줄바꿈 여부, `[n]` 이동·삭제의 진행·음성·연출 안전성, 초과 증상, RSC의 실제 제한 대상과 단위, 긴 한글 대사의 구동 안정성을 별도 기술 작업에서 검증해 게임별 출력 규격으로 기록합니다. 해당 프로젝트가 이를 '6단계'로 정했다면 그 단계의 최우선 합격 조건이며, 1차 언어 검수에 전가하지 않습니다.

Pretendard Bold를 승인한 작품은 그 폰트의 실제 적용 크기와 게임 렌더링 조건으로 측정합니다. 다른 작품은 각각의 승인 폰트를 사용합니다. **글자 수, 화면 픽셀 폭, 최대 줄 수, RSC는 같은 검사가 아닙니다.**

```text
3차 언어 최종 수정
→ 검사기의 실제 픽셀 폭 계산과 줄바꿈 후보 생성
→ AI/검수자의 한국어 호흡·조사·어절·문장부호 확인
→ 수정 행 재계산과 RSC·보호 필드 별도 검사
→ 합격한 번역문과 배치 확정
```

검사기·정확한 출력 규격을 사용할 수 없으면 언어 결과만 반환하고 로컬 배치 검증 대기로 남깁니다. 초과를 숨기려고 의미 삭제, 문장 절단, 무단 폰트 축소나 페이지 명령 삽입을 하지 않습니다. 폰트·문장·출력 규격·빌더가 변경되면 영향받은 행을 다시 검사합니다.

**언어 검수 완료 ≠ 화면 배치 합격 ≠ RSC/구조 합격 ≠ 실제 게임 검증 완료.** 이 문서 추가만으로 특정 게임의 출력 한계나 `[n]` 안전성이 검증된 것은 아닙니다.

---

# 모델 자동 라우팅

## 현장 조직도

한글화 작업은 다음 조직도로 운영합니다.

| 역할 | 모델 | 기본 책임 |
|---|---|---|
| 소장 / 감독관 | **GPT-5.6 Sol / High** | 전체 파악, 작업 분해, 배차, 위험 판단, 결과 검증, 충돌 해결, 최종 통합 |
| 분야별 과장 / 기본 전문 실무 책임자 | **DevSpace Tunnel → Web GPT-5.6 Sol / XHIGH** | 일반~상당 난도의 실제 분석·구현·설계·조사·검토 |
| 기사 / 단순 작업자 | **GPT-5.6 Luna / XHIGH** | 범위·방법이 확정된 저위험 반복·정리·검색·테스트 |
| 외주 전문업체 | **GPT-6 Astra / Medium** | 내부 실무진이 맡기 어려운 고난도 구현·디버깅 |
| 최고급 외부 전문가 | **DevSpace Tunnel → Web GPT-6 Pro** | Astra급 접근으로도 풀기 어려운 최고난도 추론 문제 |

Terra는 기본 워크플로에서 사용하지 않습니다.

## Sol High는 기본 실무자가 아니라 감독관

Sol High는 웬만한 작업을 직접 처리할 능력이 있어도 **프로젝트 실무를 혼자 떠안는 것을 기본값으로 삼지 않습니다.**

Sol의 주업무:

- 요구사항 해석
- 프로젝트 문맥 유지
- 작업 분해
- 위험도·난이도 판정
- 작업자 배차
- 결과 취합
- 충돌 해결
- 실제 diff·테스트·런타임 결과 검증
- 최종 통합과 사용자 보고

**`Sol이 할 수 있다`, `이미 문맥을 안다`, `금방 끝낼 수 있다`만으로 전문 실무 위임을 생략하지 않습니다. Sol 사용량 절감 자체가 정당한 위임 이득입니다.**

상당한 작업인데 Sol이 모든 실무를 직접 수행한다면, 위임하지 않은 구체적인 예외 이유가 있어야 합니다.

## 기본 전문 실무자: DevSpace + Web GPT-5.6 Sol XHIGH

프로젝트 파일을 실제로 조사·분석·구현·검토하는 **비자명한 일반 실무의 기본 책임자**입니다.

예:

- 게임 파일/자산 구조 분석
- 기존 코드와 성공 자산 조사
- 추출기·빌더·패처·검수 도구의 일반 구현
- 중간~상당 난도 디버깅
- 여러 파일을 묶은 기술 조사와 설계 검토
- Sol이 계속 붙잡을 필요가 없는 전문 검토

Web Sol XHIGH를 `Luna보다 약간 어려운 일을 하는 보조 모델`로 취급하지 않습니다. **주력 실무 책임자**입니다.

## Luna XHIGH는 기사급 저위험 작업

Luna에는 다음처럼 방법과 완료 조건이 이미 확정된 일을 적극 맡깁니다.

- 파일/문자열 위치 전수 조사
- CSV/JSON/Markdown 정리
- 확정 패턴 일괄 수정
- 단순 포맷 변환
- 테스트 반복 실행
- 해시·개수·크기 비교
- 로그 집계와 후보 목록 생성

새 아키텍처, 애매한 바이너리 추론, 불명확한 원인 분석, 관계·존대·말투의 애매한 최종 판정처럼 **도면을 읽어야 하는 일은 Luna에게 맡기지 않습니다.**

## Astra Medium은 외주 전문업체

고난도 구현·디버깅에 사용합니다.

- 복잡한 바이너리·실행 파일 구조
- 저장/로드·런타임 프리징
- 어려운 패처·빌더
- 여러 계층이 얽힌 구조적 버그
- 일반 실무자가 맡기 불안한 기술 문제

기본적으로 Web Sol XHIGH 실무 범위를 넘을 때 올리지만, 처음부터 명백히 Astra급이면 일부러 Luna나 Web Sol을 실패시킨 뒤 호출하지 않습니다.

## Web GPT-6 Pro는 최고급 외부 전문가

Astra급 접근으로도 원인이 설명되지 않거나 기존 가정을 처음부터 뒤집어야 하는 최고난도 문제에 사용합니다.

불필요한 실패 단계를 만들지는 않되, 평상시 일반 실무를 Pro에 몰아주지 않습니다.

## 작업 시작 시 기본 배차

```text
사용자 요청
→ Sol High가 목표·제약·성공 조건 파악
→ 독립 하위 작업으로 분해
→ 단순·기계·저위험 작업은 Luna XHIGH
→ 일반~상당 난도 전문 실무는 DevSpace + Web GPT-5.6 Sol XHIGH
→ 고난도 구현·디버깅은 Astra Medium
→ 최고난도 난제는 DevSpace + Web GPT-6 Pro
→ Sol High가 결과 재검증·충돌 해결·최종 통합
```

같은 파일을 다룬다는 이유는 **동시 쓰기를 막는 근거**일 뿐, 모든 조사·분석·구현을 Sol이 혼자 수행해야 한다는 근거가 아닙니다. 필요한 경우 순차 위임합니다.

세부 규칙은 `workflow/SOL-LUNA-AGENT-POLICY.md`를 따릅니다.

---

# DevSpace Tunnel / Web GPT 의뢰 규칙

DevSpace는 최고난도 문제에서만 쓰는 비상 경로가 아니라, **Web GPT-5.6 Sol XHIGH를 기본 전문 실무 책임자로 쓰기 위한 일반 실무 경로**이기도 합니다.

Web Sol XHIGH를 실제 파일 작업에 사용할 때는 `workflow/WEB-SOL-DEVSPACE-OPERATING-CONTRACT.md`도 읽습니다. 현재 사용자 환경에서는 Oracle 0.20.0의 `gpt-5.6-sol / extra-high / select` 경로가 실제 웹 UI 선택, DevSpace 파일 읽기·생성·기존 파일 정밀 수정·재읽기·결과 회수까지 검증됐습니다. **Latest 기본 설명만으로 미지원이라 추정하지 않으며, 삭제·이동·이름 변경은 검증된 쓰기 범위에 포함하지 않습니다.**

계정 로그인·MFA·계정 전환·계정 전역 메모리/개인화·앱/권한/OAuth 설정은 사용자만 처리합니다. 반면 Oracle이 소유한 **현재 임시채팅 탭의 맞춤화/개인화 상태 확인과, 정확한 비활성 제어 1개를 식별했을 때 그 임시채팅 안에서만 활성화하는 동작**은 계정 전역 설정 변경이 아니며 검증된 `ensureTemporaryChatPersonalization` 계열 helper가 자동 처리할 수 있습니다. `chatgpt-workspace-setup` 또는 다른 스킬의 `user handles account personalization` 문구를 이 임시채팅 토글까지 확대해석하여 사용자에게 매 run 수동 조작을 요구하지 않습니다. 정확한 제어가 없거나 모호하거나 로그인/MFA/계정 수준 변경이 필요할 때만 자동 조작을 중단하고 사용자에게 실제 막힌 지점을 요청합니다.

Web Sol XHIGH 또는 Web GPT-6 Pro에 프로젝트 작업을 맡길 때 연결 상태를 추측하지 않습니다.

## 기본 순서

1. Sol High가 먼저 작업 목표와 하위 작업을 분해합니다.
2. 전문 실무로 분리 가능한 작업은 Web GPT-5.6 Sol XHIGH 배차를 기본 검토합니다.
3. 대상 프로젝트의 정확한 루트를 확인합니다.
4. **DevSpace Tunnel의 `open_workspace`를 실제 호출합니다.**
5. 성공하면 해당 workspace를 기준으로 Web GPT 작업을 즉시 진행합니다.
6. 현재 환경이 Web GPT 자동 의뢰 동작을 실제로 제공하면 자동 전달을 우선합니다.
7. 자동 전달 기능이 없거나 사용자 동작이 필수이면 Sol이 의뢰 준비를 모두 끝낸 뒤 사용자에게 최소 동작만 요청합니다.

`Sol이 직접 해도 된다`는 이유로 DevSpace 실무 배차를 생략하지 않습니다. 반대로 사용량을 쓰기 위한 형식적 호출도 하지 않습니다. **전문 실무를 적절한 책임자에게 맡겨 Sol의 고비용 추론을 아끼는 것**이 목적입니다.

사용자에게 전달할 의뢰에는 최소한 다음을 포함합니다.

- 작업 목표
- 필요한 프로젝트 경로와 파일
- 현재까지 확인된 사실
- 이미 시도했거나 실패한 접근
- 건드리면 안 되는 범위
- 원하는 결과 형식
- 후속 작업자가 바로 이어갈 수 있는 출력 위치 또는 회수 방법

사용자에게 문제를 처음부터 다시 설명하거나 프롬프트를 새로 작성하게 하지 않습니다.

## DevSpace 오류 판정

### `tool disabled` / `FORBIDDEN`

- 네트워크 장애로 추측하지 않습니다.
- 현재 ChatGPT 세션 또는 앱에서 developer MCP 사용이 활성화되지 않은 상태로 판정합니다.
- DevSpace 수동 실행, 재설치, Tailscale, Watchdog, OAuth 변경을 지시하지 않습니다.

### `502` / `network_error`

- 실제 MCP 연결 경로 문제 가능성이 있습니다.
- 일시 장애 가능성을 고려해 **최소 1분 간격으로 최대 5회 재시도**합니다.
- 5회 후에도 실패할 때만 MCP 연결 경로를 조사하거나 사용자에게 상태 확인을 요청합니다.

실제 `open_workspace` 호출 없이 "연결됨" 또는 "연결 끊김"을 단정하지 않습니다.

## Web GPT 작업자 역할 고정

Oracle 또는 이에 준하는 외부 실행 경로로 Web GPT를 호출할 때, 미션에는 다음 역할 경계를 명시합니다.

```text
당신은 이 미션을 직접 수행하도록 호출된 최종 작업 수행자다.
별도 지시가 없는 한 Oracle이나 다른 GPT를 호출하지 마라.
호스트 Codex/Oracle의 실행 완료를 기다리지 마라. 호스트가 바로 당신의 응답을 기다리고 있을 수 있다.
지정된 프로젝트 자료와 허용된 외부 조사 범위 안에서 작업을 직접 수행하고 결과를 반환하라.
직접 수행할 수 없다면 다른 작업자에게 재위임하지 말고, 현재 막힌 이유와 필요한 입력만 반환하라.
```

이 규칙의 목적은 Web GPT가 자신에게 적용된 자동화 스킬을 다시 실행하여 Oracle → Web GPT → Oracle 형태의 순환 대기를 만드는 것을 막는 것입니다.

## 자동 위임 1회 안전장치

Web GPT 작업자 자체의 역할 혼동 또는 작업 수행 실패에 대해서는 **동일 미션의 자동 재위임을 반복하지 않습니다.**

다음 중 하나가 발생하면 해당 자동 위임은 1회에서 종료합니다.

- Web GPT가 다시 Oracle 또는 다른 GPT를 호출하려 함
- 자기 호스트 Oracle/Codex의 완료 상태를 기다림
- 중첩 호출이 프로젝트 락에 막힘
- 지정된 작업을 직접 수행하지 않고 다시 상위 작업자에게 반환함
- 실질적인 조사/분석 결과 없이 역할 혼동 상태로 종료함

이 경우 Sol은 다음 순서로 처리합니다.

1. 자동 재위임을 중단합니다.
2. 사용자에게 어떤 역할 혼동 또는 수행 실패가 발생했는지 짧게 보고합니다.
3. 대상 모델, 작업 목표, 프로젝트 경로, 필요한 자료, 금지 범위, 원하는 출력 형식까지 포함한 **완성된 수동 전달 프롬프트**를 준비합니다.
4. 사용자에게 해당 프롬프트를 Web GPT에 그대로 보내도록 최소 동작만 요청합니다.
5. 사용자가 답변을 가져오거나 프로젝트에 결과를 저장하면 Sol이 그 결과를 검토하고 기존 작업을 이어갑니다.

사용자가 Web GPT에 직접 미션을 전달하는 것은 실패가 아니라 정상적인 **manual handoff fallback**으로 취급합니다.
사용자에게 문제를 처음부터 다시 설명하거나 새 프롬프트를 작성하게 하지 않습니다.

### 연결 장애와 작업자 실패를 구분

다음 둘은 같은 재시도 정책을 사용하지 않습니다.

```text
DevSpace/MCP 502 또는 network_error
  → 연결 장애 규칙에 따라 최소 1분 간격, 최대 5회 재시도

Web GPT 역할 혼동 / 자기 재호출 / 호스트 대기 / 무의미한 반환
  → 자동 위임 1회에서 종료하고 사용자에게 manual handoff 요청
```

연결이 살아 있다는 사실만으로 작업 수행이 성공했다고 판단하지 않습니다.
Web GPT 답변을 회수했다는 사실만으로 답변 내용의 정확성이나 실제 구현 성공까지 보증되었다고 판단하지 않습니다.

---

# 위임·검증 공통 규칙

## 위임이 기본인 실무와 Sol 직접 수행 예외

이 정책에서는 **Sol 사용량 절감 자체가 실질적인 위임 이득**입니다.

일반~상당 난도의 전문 실무를 Web Sol XHIGH에 독립적으로 맡길 수 있으면 위임을 우선합니다. 단순·기계·저위험 부분은 Luna로 떼어냅니다.

Sol이 직접 실무까지 수행하는 합리적인 예외:

- 작업 분해·판정·통합·최종 검토 자체
- 사용자와의 짧은 의사결정
- handoff 설명보다 작은 명백한 초소형 수정
- 다른 작업자의 결과를 합치는 짧은 통합 수정
- 실제 작업 경로가 제공되지 않고 수동 handoff보다 직접 처리하는 편이 명백히 나은 작은 작업

다음은 **위임 생략 사유가 아닙니다.**

- `Sol도 할 수 있음`
- `Sol이 이미 문맥을 알고 있음`
- `Sol이 금방 끝낼 수 있음`
- `같은 파일을 다룸`이라는 이유만으로 전체 실무를 Sol이 독점하는 것

같은 파일은 동시 쓰기를 피하되 조사·분석·검토를 분리하거나 순차 위임할 수 있습니다.

상당한 작업인데 Sol이 모든 실무를 직접 수행했다면 결과 보고에 구체적인 예외 이유를 남깁니다.

## 병렬 작업

병렬 위임은 서로 독립적인 작업에만 사용합니다.

좋은 예:

```text
Luna A → 옵션 텍스처 구조 조사
Luna B → 기존 빌더 조사
Luna C → 테스트 로그 정리
```

피해야 할 예:

```text
두 작업자가 동시에 같은 빌더 수정
같은 staging 폴더에 동시에 쓰기
한 작업 결과가 다른 작업의 입력인데 병렬 실행
```

## 결과를 받았다고 완료가 아님

```text
AI 응답 완료
≠ 구현 완료
≠ 정적 검증 완료
≠ 실제 게임 검증 완료
```

Sol은 다른 모델의 결과를 실제 프로젝트 상태와 비교하고 필요한 테스트를 다시 확인한 뒤 반영합니다.

---

## Google Drive 검수 구조

게임별 Drive 작업공간의 권장 구조:

```text
게임명/
├─ 00_공통기준/
│  ├─ PRE_TRANSLATION_SETUP.md
│  ├─ glossary.csv
│  ├─ characters.csv
│  ├─ relationships.csv
│  └─ speech_style.csv
├─ 01_1차검수/
│  └─ INSTRUCTION_1ST.md
├─ 02_2차검수/
│  └─ INSTRUCTION_2ND.md
├─ 03_3차검수/
│  └─ INSTRUCTION_3RD.md
└─ 04_최종/
   └─ final reviewed CSV
```

실제 작품 폴더가 다른 이름이나 기존 구조를 이미 사용한다면 폴더를 억지로 재배치하지 않고 동일 역할의 파일을 연결합니다.

검수 시작 전에 0단계 기준자료를 먼저 구축합니다. 용어집, 캐릭터 이름·성격·말투, 화자→청자 관계, 반말/존댓말, 호칭과 근거 상태가 핵심입니다.

공통 기준에는 최초 출력 시험에서 얻은 게임별 `layout_profile` 또는 동등한 출력/진행 규격도 함께 등록합니다. 가로폭·조건부 글자 수·최대 줄 수·`[n]` 동작별 허가·반각/전각/공백 보존 규칙과 규격 버전을 포함하고, 각 차수 지시문이 자기 역할에 맞게 이를 참조하도록 생성·갱신합니다. 실제 게임 규격을 측정하지 않은 채 다른 작품의 값을 복사하지 않습니다.

각 단계는 목적이 다르므로 지시문도 물리적으로 분리합니다.

- 1차: 전체 대사집을 **1,000행 단위**로 기초 규칙·구조·관계·존대·용어 검수
- 2차: 1차 결과 전체를 다시 **1,000행 단위**로 자연스러운 한국어와 캐릭터 말투 보정, 최종 배치 미확정
- 3차: 2차 결과 전체를 다시 **500행 단위**로 가장 엄격한 최종 정밀검수 후 `[n]`·픽셀 폭·최대 줄 수·RSC 검사

세부 규칙은 다음 문서를 사용합니다.

- [최초 한글 출력·진행 안전성 사전검증](workflow/EARLY-TEXT-RUNTIME-GATE.md)
- [번역 전 기준자료·용어집 선세팅 정책](review/PRE-TRANSLATION-SETUP.md)
- [대사 번역 공통 원칙](review/DIALOGUE-TRANSLATION-POLICY.md)
- `review/REVIEW-DATA-SCHEMA.md`
- `review/REVIEW-STAGE-1.md`
- `review/REVIEW-STAGE-2.md`
- `review/REVIEW-STAGE-3.md`

1차·2차·3차의 지시문은 서로 섞지 않습니다.
각 단계는 앞 단계의 결과 데이터를 사용할 수 있지만 다른 단계의 지시문을 그대로 참조하여 목적을 흐리지 않습니다.

---

## 실제 플레이 피드백

AI 제안은 최종 진실로 취급하지 않습니다.

기본 우선순위:

1. 실제 게임 플레이 중 사용자가 직접 확인하고 확정한 수정
2. 게임 내 실제 문맥과 대사 근거
3. 등록된 용어집·캐릭터·관계도·말투 규칙
4. AI 분석 또는 번역 제안

어떤 모델도 AI 판단만으로 1번 값을 덮어쓰지 않습니다.

세부 정책은 `workflow/PLAYTEST-FEEDBACK-POLICY.md`를 따릅니다.

---

## 모델 또는 기능을 사용할 수 없을 때

문서에 적힌 모델이나 호출 경로가 현재 실행 환경에서 실제로 제공되는지 확인합니다.

- 사용자가 정한 모델/추론 강도를 임의의 다른 모델로 조용히 대체하지 않습니다.
- 전문 실무의 기본 작업자를 사용할 수 없다고 해서 상당한 작업을 자동으로 Sol에게 몰아주지 않습니다. 작은 작업은 Sol이 흡수할 수 있지만, 상당한 Web Sol XHIGH급 작업은 가능한 경우 manual handoff를 먼저 준비합니다.
- Web GPT 수동 전달이 필요하면 Sol이 완성된 프롬프트를 준비합니다.
- 새 유료 API, 새 공급자, 새 외부 데이터 전송을 자동으로 시작하지 않습니다.
- 실행하지 않은 모델 호출을 실행했다고 보고하지 않습니다.

---

## 템플릿

새 프로젝트를 만들 때 다음 템플릿을 복사해서 게임별로 채울 수 있습니다.

- `templates/AGENTS.template.md`
- `templates/glossary.template.csv`
- `templates/characters.template.csv`
- `templates/relationships.template.csv`
- `templates/speech-style.template.csv`

---

## 공개 저장소 주의

이 아카이브에는 다음 자료를 넣지 않습니다.

- 게임 본편 또는 원본 RomFS
- NSP/XCI/NCA
- 키 파일
- 원작 이미지·음성·영상의 대량 원본
- 전체 원문 스크립트 덤프
- 개인 PC 절대 경로
- 토큰·비밀번호·Secret
- 개인 서버나 계정 식별정보

새 프로젝트에서 얻은 노하우를 이 아카이브로 되돌릴 때는 **다른 게임에서도 재사용 가능한 원리와 실패/성공 근거만 추려서** 기록합니다.

---

# 한 줄 요약

사용자가 이 문서 링크와 한글화 작업만 전달하면:

```text
신규 게임이면 먼저 D:\Codex\한글화 프로젝트\<게임명> 전용 폴더를 만들고 모든 로컬 작업을 그 안에서 시작한다
→ Sol High 소장이 프로젝트를 파악하고 작업을 분해·배차한다
→ 최초 한글 출력 때 가로폭·줄 수·[n]·반각/전각과 다음 장면 진행을 시험하고 착수 조건을 정한다
→ 본 번역 전 용어집·캐릭터·관계·말투 0단계 기준자료를 먼저 구축한다
→ 대사 검수는 전체 데이터를 1차 1,000행 → 2차 1,000행 → 3차 500행 정밀검수로 다시 훑는다
→ 일반~상당 난도의 실제 전문 실무는 DevSpace + Web GPT-5.6 Sol XHIGH 과장급이 담당한다
→ 안전한 단순·반복 작업만 Luna XHIGH 기사급이 담당한다
→ 내부 실무진이 맡기 어려운 고난도 구현·디버깅은 Astra Medium 외주급으로 올린다
→ 그래도 해결하기 어려운 최고난도 문제는 DevSpace + Web GPT-6 Pro 최고 전문가에게 올린다
→ Web GPT 자동 위임이 역할 혼동으로 실패하면 자동 재귀 호출 없이 사용자 manual handoff
→ Sol High가 결과를 다시 검증하고 충돌을 해결해 최종 통합한다
```

**사용자는 모델 배차를 매 작업마다 다시 지시할 필요가 없습니다.**
